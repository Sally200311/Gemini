name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install and Build 🔧
        # 移除 cache 設定以避免 lock file 錯誤
        # 直接安裝並打包
        run: |
          npm install
          npm run build
        env:
          # 注入環境變數
          VITE_FINNHUB_API_KEY: ${{ secrets.VITE_FINNHUB_API_KEY }}
          API_KEY: ${{ secrets.API_KEY }}

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Vite 打包後的產出資料夾
          branch: gh-pages # 推送到這個分支
          clean: true # 自動清除舊檔案
